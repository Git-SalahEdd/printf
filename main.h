#ifndef MAIN_H
#define MAIN_H
#define NULL_STRING "(null)"

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>
#include <limits.h>
#include <unistd.h>

int _putchar(char c);
int _printf(const char *format, ...);
void myloop(const char *f, ...);
#endif
