#ifndef MAIN_H
#define MAIN_H
#define NULL_STRING "(null)"
#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h>

int _putchar(char c);
int _printf(const char *format, ...);
void chk(const char *format);

#endif
